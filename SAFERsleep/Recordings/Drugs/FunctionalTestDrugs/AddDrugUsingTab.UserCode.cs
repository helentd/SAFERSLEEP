///////////////////////////////////////////////////////////////////////////////
//
// This file was automatically generated by RANOREX.
// Your custom recording code should go in this file.
// The designer will only add methods to this file, so your custom code won't be overwritten.
// http://www.ranorex.com
//
///////////////////////////////////////////////////////////////////////////////

using System;
using System.Collections.Generic;
using System.Text;
using System.Text.RegularExpressions;
using System.Drawing;
using System.Threading;
using WinForms = System.Windows.Forms;

using Ranorex;
using Ranorex.Core;
using Ranorex.Core.Repository;
using Ranorex.Core.Testing;

namespace SAFERsleep.Recordings.Drugs.FunctionalTestDrugs
{
    public partial class AddDrugUsingTab
    {
        /// <summary>
        /// This method gets called right after the recording has been started.
        /// It can be used to execute recording specific initialization code.
        /// </summary>
        private void Init()
        {
            // Your recording specific initialization code goes here.
        }

        public void EnterDosageUsingTab(string Mode, string Dosage)
        {
            Ranorex.Report.Info("Enter Dosage by tab");
        	
        	if (Mode == "Bolus")
        	{
        		Keyboard.Press("{Tab}");
        	    // Keyboard.Press(System.Windows.Forms.Keys.Tab, Keyboard.DefaultScanCode, Keyboard.DefaultKeyPressTime, 1, true);
                Delay.Milliseconds(0);
        		Keyboard.Press(Dosage);
        	}
        	else if (Mode == "Infusion")
        	{
        		// press tab 2 times
        		Keyboard.Press("{Tab}");
        		Keyboard.Press("{Tab}");
                Delay.Milliseconds(0);
        		Keyboard.Press(Dosage);
        	}
        	else if (Mode=="DefaultScript_Bolus" || Mode=="DefaultScript_Infusion")
        	{// If Mode is  DefaultScript, no need to input Dose/Rate
        		Ranorex.Report.Info("Dose/Rate is selected as Default script");
        	}
        	else if (Mode=="InfusionPurge")
        	{ // If Mode is  DefaultScript, no need to input Rate
        		Ranorex.Report.Info("No Rate input as Mode is purge");
        	}
        	
        	Delay.Milliseconds(200);
        }

        public void EnterUnitUsingTab(string Mode, string Unit)
        {
            Ranorex.Report.Info("Select Unit");        	
            if (Mode == "Bolus")        	
            {	
            	 //press tab to Unit field
	        	Keyboard.Press("{Tab}");
	        	Delay.Milliseconds(200);
				// delete the current value	        	
	        	SAFERsleep.UserCodeCollection_OR.Clear_TextBox(repo.FrmEditDrug.UnitBolusInfo,"Combobox");
	        	// enter the value
	        	Keyboard.Press(Unit);
	        	Delay.Milliseconds(200);
	        	
	        	// press "Enter" to add the current value         	
        		Keyboard.Press("{Enter}");
        		Delay.Milliseconds(200); 
            	
            }
        	else if (Mode == "Infusion")        	
        	{	 
	 			//press tab to Unit field
	        	Keyboard.Press("{Tab}");
	        	Delay.Milliseconds(200);        		
        		// delete the current value	        	
	        	SAFERsleep.UserCodeCollection_OR.Clear_TextBox(repo.FrmEditDrug.UnitInfusionInfo,"Combobox");
	        	
	        	// enter the value
	        	Keyboard.Press(Unit);
	        	Delay.Milliseconds(200);
	        	
	        	// press "Enter" to add the current value
	        	Keyboard.Press("{Enter}");
				Delay.Milliseconds(200);   		
        	}  
			else if (Mode=="InfusionPurge" || Mode=="DefaultScript_Bolus" || Mode=="DefaultScript_Infusion") 
			{ // If Mode is  InfusionPurge/Default script, no need to input unit
        		Ranorex.Report.Info("No Unit inut as Mode is InfusionPurge/Default script");
        	} 
        }

        public void EnterCommentUsingTab(string Comment)
        {
            //press tab to Comment field
        	Keyboard.Press("{Tab}");
        	Delay.Milliseconds(200);
        	// delete the current value	        	
	        SAFERsleep.UserCodeCollection_OR.Clear_TextBox(repo.FrmEditDrug.CommentInfo,"Text");
	        // enter Comment
	        Keyboard.Press(Comment);
            Delay.Milliseconds(100);
        }

        public void EnterRouteUsingTab(string Route)
        {
            Ranorex.Report.Info("Select Route");
        	
        	//press tab to Route field
        	Keyboard.Press("{Tab}");
        	Delay.Milliseconds(200);
        	// delete the current value	        	
	        SAFERsleep.UserCodeCollection_OR.Clear_TextBox(repo.FrmEditDrug.RouteInfo,"Combobox");
        	
        	// enter the value
        	Keyboard.Press(Route);
        	
        	// press "Enter" to add the current value
        	Keyboard.Press("{Enter}");
        	
        	Delay.Milliseconds(200);
        }

    }
}
